<div class="container">
<p>Create a new list</p>

New list title
<%= simple_form_for [@list] do |f| %>
    <%= f.input :name %>
    <%= f.submit 'Submit', class: 'btn btn-danger mt-2' %>
  <% end %>

</div>
